import 'package:flutter/material.dart';

class SessionMaterialDetailTableUnit extends StatelessWidget {
  const SessionMaterialDetailTableUnit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 70,
    );
  }
}
